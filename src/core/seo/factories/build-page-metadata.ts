/**
 * Re-export of the metadata factory from a path that mirrors the folder
 * structure required by Addendum 002 §4 (Factory location convention).
 */
export { buildPageMetadata } from "../build-page-metadata";
export type { PageMetadataInput } from "../build-page-metadata";
